[package]
name = "qpl"
version = "0.1.1"
edition = "2024"

[dependencies]
clap = { version = "4.6.6", features = ["derive"] }
polars = { version = "0.55.2", features = ["lazy", "parquet", "csv", "strings", "is_in", "abs"] }
rustyline = "18.0.1"
